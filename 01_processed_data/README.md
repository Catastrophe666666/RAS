# Processed Data

This folder keeps the lightweight data handoff.

It includes core metadata and the master article-text dataset. It does not include cleaned per-article txt files or large topic-modeling cache files.

