# China Journal Note

The China Journal has not yet been OCRed, segmented into articles, or converted into structured metadata.

It should not be described as part of the current analytical corpus. The RAS Journal workflow can instead serve as a model for a future China Journal pipeline:

```text
scanned pages
        -> OCR
article segmentation
        -> metadata extraction
structured article records
        -> topic modeling and network analysis
```

